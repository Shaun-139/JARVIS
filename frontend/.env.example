# Only needed when the frontend and backend run on different origins (e.g.
# frontend on Vercel, backend on Render). Point it at the deployed backend.
# Leave unset for local dev — Vite proxies /api to the backend on localhost.
VITE_API_BASE=
